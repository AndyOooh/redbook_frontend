// import './Card.scss';

// export const Card = props => {
//   // const cardClasses = props.extraClasses ? `card ${props.extraClasses.join(' ')}` : 'card';
//   const cardClasses = props.extraClasses ? `card ${props.extraClasses}` : 'card';
//   return <div className={cardClasses}>{props.children} </div>;
// };
